services:
  db:
    image: mysql:5.6
    container_name: mysql-opensip
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: opensip
      MYSQL_DATABASE: opensips_db
    volumes:
      - /home/tcc/projeto_tcc/mysql_files:/var/lib/mysql
